root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true

# Two trailing spaces in Markdown create a <br> — don't trim
[*.md]
trim_trailing_whitespace = false

[*.{yaml,yml,toml}]
indent_style = space
indent_size = 2
